API_KEY="the api key to the llm, may be ampty if not needed"
BASE_URL="url to connect to the llm, if using lmstudio is http://127.0.0.1:1234/v1 para ollama debe ser similar"
MODEL_NAME="the selected model to use"